language: php

php:
  - 7.1
  - 7.2

before_script:
  - cp .env.test .env
  - composer self-update
  - composer install --prefer-source --no-interaction --dev
  - php artisan key:generate

script:
  - phpunit

services:
  - mysql

notifications:
